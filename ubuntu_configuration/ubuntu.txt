
https://installlion.com/ubuntu/precise/universe/m/index.html

Rename file & Move File:
	sudo mv "old location" "new location"
Copy File:
	sudo cp "Original file" "new File"
Remove File:
	sudo rm -f filename

1) By using -i for interactive you will be asked if you would like to replace the file:
	cp -i /home/levan/kdenlive/untitelds.mpg /media/sda3/SkyDrive/
	
or you can use -b to create a backup of your file:
	cp -b /home/levan/kdenlive/untitelds.mpg /media/sda3/SkyDrive

2) Same as the above:
	cp (-i or -b) /media/sda3/SkyDrive/untitelds.mpg /home/levan/kdenlive

3) Use -R for recursive and -i for interactive:
	cp -Ri ~/MyFolder /sda3/

4) This last one can be done via the mv command, move is like cutting:
	mv -i ~/MyFile ~/OtherFolder/MyFile

if you want to move a directory, use:
	mv -Ri ~/MyDirectory ~/OtherDirectory/

show packge
sudo dpkg -L <packge>

get url 
pwd

Downloads install:
	pwd
	cd /downloads
	./<filename>
	
	
sudo apt update -y
sudo nano index.html
sudo /etc/init.d/apache2 stop
ifconfig

((( Ubuntu system restart )))
sudo reboot
sudo shutdown -r


/// Rename Hostname ///
sudo hostnamectl set-hostname sisnet-u20.cis-rostock.de


//// ubunu infos ////
lsb_release -a



/// postgress ////
user: postgres
password: root
